Написать функцию, проверяющую правильность расстановки круглых скобок () в строке:

function checkBrackets(s: string): boolean {
    // code
}

// checkBrackets('(') === false
// checkBrackets(')') === false
// checkBrackets('()') === true
// checkBrackets('(())') === true
// checkBrackets(')(') === false
// checkBrackets(')()(') === false